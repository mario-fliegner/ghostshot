<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
         SafMimicContentProvider and PhotoPickerMimicContentProvider have been moved to
         app/src/debug/ so their classes live in the app APK's classloader. This fixes
         ClassNotFoundException at ContentProvider instantiation: the app process's
         Application.getClassLoader() cannot reach secondary DEX shards in the test APK,
         and the providers were landing in shard 11 of the test APK's project DEX.
    -->
    <application />
</manifest>
